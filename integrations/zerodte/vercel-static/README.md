# Vercel Adaptive Loop static assets

Minified Command Center assets for `0-dte` while `cursor[bot]` cannot push
to `DGator86/0DTE`. The thin Vercel deploy loads these from jsDelivr.

Prefer merging `../ready/0001-embed-spy-der-adaptive-loop-on-vercel.patch`
into 0DTE so Vercel builds from that repo directly.
